# zed
project(helloworld) # _exe를 빼고 깔끔하게 작성

# 프로젝트 이름(quick_sort) 그대로 실행 파일이 생성됩니다.
add_executable(${PROJECT_NAME} main.cpp)

# vscode
# # 실행 파일명을 'hello_exe'로 지정하고 main.cpp를 컴파일합니다.
# add_executable(hello_exe main.cpp)
